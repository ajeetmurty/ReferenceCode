package ajeetmurty.refcode.java.template.io;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class RJFile {
	private final Logger logp = LoggerFactory.getLogger(this.getClass().getName());
}